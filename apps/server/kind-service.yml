apiVersion: v1
kind: Service
metadata:
  name: pfn-job-application-tracker-server
spec:
  type: NodePort
  selector:
    app: pfn-job-application-tracker-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
